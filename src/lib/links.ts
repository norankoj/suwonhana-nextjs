/**
 * 외부 구글폼 주소.
 *
 * 같은 플레이스홀더 주소(1Z1Z1Z…)가 세 파일에 복사돼 있어서
 * 클릭하면 전부 404 로 갔다. 한 곳으로 모으고, 값이 비어 있으면
 * 각 화면에서 버튼 자체를 렌더하지 않는다 — 깨진 링크보다 없는 편이 낫다.
 *
 * 실제 폼을 만들면 아래 문자열만 채우면 된다.
 */

/** 기부금(연말정산) 영수증 신청 폼 */
export const RECEIPT_FORM_URL = "";

/** 새가족 질문 접수 폼 */
export const QUESTION_FORM_URL = "";
